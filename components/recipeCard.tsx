import { View, Button, Text } from "react-native";

export default function RecipeCard({ recipe }: { recipe: RecipeCard }) {
    return (
        <View className="bg-slate-500 h-5/6 w-full rounded-md">
            <Text>hi</Text>
        </View>
    );
}
